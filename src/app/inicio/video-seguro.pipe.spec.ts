import { VideoSeguroPipe } from './video-seguro.pipe';

describe('VideoSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
